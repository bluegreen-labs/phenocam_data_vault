# this file resides in /etc/default/gpsd
# Default settings for gpsd.
# Please do not edit this file directly - use `dpkg-reconfigure gpsd' to
# change the options.

# start on boot, with 
START_DAEMON="true"

# for the NTP server to work the device should
# not wait for clients to connect as it has to serve
# up results immediately to any connecting NTP client
GPSD_OPTIONS="-n"

# These are the settings for the adafruit GPS shield
DEVICES="/dev/ttyAMA0"
USBAUTO="false"
GPSD_SOCKET="/var/run/gpsd.sock"

